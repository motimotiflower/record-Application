<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sample3"
        tools:targetApi="31">
        <activity
            android:name=".Main.MainActivity"
            android:exported="true"
            >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--memo-->
        <activity android:name=".memo.home.activity.MemoHomeActivity">
        </activity>

        <activity android:name=".memo.edit.activity.MemoEditActivity">
        </activity>


        <!--todo-->
        <activity android:name=".Todo.home.activity.TodoHomeActivity">
        </activity>

        <activity android:name=".Todo.edit.activity.TodoEditActivity">
        </activity>


        <!--CatFile-->
        <activity android:name=".CatFile.home.activity.CatHomeActivity">
        </activity>

        <activity android:name=".CatFile.select.activity.CatSelectActivity">
        </activity>

        <activity android:name=".CatFile.edit.activity.CatEditActivity">
        </activity>

        <activity android:name=".CatFile.select.activity.FileSelectActivity">
        </activity>

        <activity android:name=".CatFile.edit.activity.FileEditActivity">
        </activity>




    </application>

</manifest>